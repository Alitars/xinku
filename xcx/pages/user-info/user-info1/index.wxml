<!--pages/user-info1/userinfo1.wxml-->
<view class="container">
  
  <view class="header" style="background-image: url(https://xinkush.vipcaihui.cn/image/M00/00/04/rB_sZV9v60iAedVIAAG8krdVClg202.png)">
    <view class="avatar-wrap">
      <view class="avatar" bindtap="uploadAvatar">
        <van-image width="200rpx" height="200rpx" src="{{avatarUrl}}" />
      </view>
      <text class="name">{{name}}</text>
    </view>
    
    
  </view>
  <view class="content">
    <view class="form">
    <van-cell-group>
      <van-cell title="个人信息（必填）" 
        link-type="navigateTo"
        url="../user-info2/index">
        <van-icon slot="right-icon" name="edit"/>
      </van-cell>
      <van-cell title="教育经历">
        <van-icon slot="right-icon" name="edit"/>
      </van-cell>
      <van-cell title="求职意向">
        <van-icon slot="right-icon" name="edit"/>
      </van-cell>
      <van-cell title="自我评价">
        <van-icon slot="right-icon" name="edit"/>
      </van-cell>
      <van-cell title="专业证书">
        <van-icon slot="right-icon" name="edit"/>
      </van-cell>
      <van-cell title="专业技能">
        <van-icon slot="right-icon" name="edit"/>
      </van-cell>
      <van-cell title="上传作品">
        <van-icon slot="right-icon" name="edit"/>
      </van-cell>
      <van-cell title="培训经历">
        <van-icon slot="right-icon" name="edit"/>
      </van-cell>
      <van-cell title="实习经历">
        <van-icon slot="right-icon" name="edit"/>
      </van-cell>
    </van-cell-group>
      <!-- <van-field
        value="{{ form.userInfo }}"
        placeholder="个人信息（必填）"
        border="{{ true }}"
        right-icon="edit"
        error-message="{{phoneErrMsg}}"
        bind:change="onFormChange" 
        value="{{form.phone}}" 
        id="phone" 
      />
      <van-field
        value="{{ value }}"
        placeholder="教育经历"
        border="{{ true }}"
        right-icon="edit"
        error-message="{{phoneErrMsg}}"
        bind:change="onFormChange" 
        value="{{form.phone}}" 
        id="phone" 
      />
      <van-field
        value="{{ value }}"
        placeholder="求职意向"
        border="{{ true }}"
        right-icon="edit"
        error-message="{{phoneErrMsg}}"
        bind:change="onFormChange" 
        value="{{form.phone}}" 
        id="phone" 
      />
      <van-field
        value="{{ value }}"
        placeholder="自我评价"
        border="{{ true }}"
        right-icon="edit"
        error-message="{{phoneErrMsg}}"
        bind:change="onFormChange" 
        value="{{form.phone}}" 
        id="phone" 
      />
      <van-field
        value="{{ value }}"
        placeholder="专业证书"
        border="{{ false }}"
        border="{{ true }}"
        right-icon="edit"
        error-message="{{phoneErrMsg}}"
        bind:change="onFormChange" 
        value="{{form.phone}}" 
        id="phone" 
      />
      <view class="btn-wrap margin-bottom-40 margin-top-80">
        <van-button type="info" block class="margin-bottom" bind:click="upShopLogo">
          保存
        </van-button>
      </view> -->
    </view>
  </view>
</view>